assignments.conf defines all the keybindings and shortcuts (i.e. Increase volume, launch app launcher, etc.)
workspaces.conf defines the configuration related to workspaces in a multimonitor setup
environment.conf is related to the way the UI behaves
